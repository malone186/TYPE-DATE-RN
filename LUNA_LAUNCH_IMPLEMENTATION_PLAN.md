# TYPE DATE — Codex Luna extra high 실행 기획안

> 작성: 2026-08-31 · 작업 폴더: `C:\1\TYPE-DATE-RN`
> 검토 기준: HEAD `f16249b`, 로컬 소스·설정·SQL·개인정보 초안
> 실행 대상: **GPT-5.6 Luna (`gpt-5.6-luna`), extra high (`xhigh`)**
> 이 문서는 후속 구현용 명세다. 작성 시점에는 앱 구현·원격 변경·배포를 실행하지 않았다.

## 1. 사용 방법과 범위

Codex에서 Luna와 extra high를 선택한 뒤 아래 “전달용 시작 프롬프트”를 붙여 넣는다. 모델 이름과 지원 설정은 이 세션에 제공된 앱 도구 메타데이터를 기준으로 했다. 프롬프트에 모델 이름을 쓰는 것만으로 실제 실행 모델이 바뀌지는 않는다. 다른 환경에서 해당 모델이 없으면 임의로 대체하지 말고 알려준다.

`xhigh` 설정명은 [OpenAI 공식 설정 문서](https://learn.chatgpt.com/docs/config-file/config-reference)에 설명되어 있으며 지원 여부는 모델에 따라 다르다. 전역 Codex 설정 파일은 이번 작업 때문에 변경하지 않는다.

**목표:** 기존 게임과 디자인을 유지하면서 Android 출시 전 결제·광고·통계·개인정보의 잘못된 동작을 수정하고, 무엇이 검증됐고 무엇이 아직 외부 준비를 기다리는지 분명하게 만든다.

**계획상 가정:**

- Android 우선이다. 웹 개발 실행은 계속 가능해야 한다. iOS 기존 코드를 불필요하게 깨뜨리지 않되 iOS 출시/ATT 구현은 별도 요청 전까지 확장하지 않는다.
- `com.typedate.app`과 `remove_ads`를 유지한다.
- 광고 제거는 비소모성 일회성 구매다. 한국 목표 가격은 2,200원이지만 표시 가격의 기준은 스토어 상품 정보다.
- 전면 광고는 기존 데이트 종료→결과 보고서 사이 한 곳만 사용한다. 배너·보상형·앱 시작 광고·추가 광고 지면은 만들지 않는다.
- SNS 공유 기능, 스토리·캐릭터·게임 밸런스 변경, 디자인 개편은 제외한다.
- 현재 미완료 기능을 “이미 끝났다”고 전제하지 않는다.
- 서버 결제 검증은 유료 출시를 위한 보안 요구로 포함한다. 로그인/회원가입 화면을 새로 만들지는 않는다.
- 실계정·콘솔 설정·실제 결제·비공개 테스트·스토어 승인은 코드 작업만으로 완료할 수 없다.

## 2. 원안에서 고친 문제와 근거

| 우선순위 | 확인된 문제 | 파일/근거 | 수정 방향 |
|---|---|---|---|
| P0 | 타입 검사 통과를 전제했으나 현재 실패 | `npm.cmd run typecheck`: expo-iap TS2307 2건, 파생 TS7006 1건 | 잠금 파일 기준 의존성 동기화 후 다시 판정. any로 우회 금지 |
| P0 | 공개 키를 JWT로 취급할 가능성 | `src/analytics/track.ts`는 KEY를 apikey와 Bearer 모두에 넣음. 원안 예시는 sb_publishable 계열 | 공개 키 인증 규칙에 맞게 수정하고 HTTP 실패 확인 |
| P0 | 구매/복원 상태 검사가 비대칭 | `billing.ts`의 리스너는 purchased 검사, 자동 복원은 productId만 검사 | 공통 자격 판정으로 미결제 상태 권한 부여 방지 |
| P0 | 구매 확인이 클라이언트 콜백에만 의존 | `grantRemoveAds`, 로컬 td_ad_removed | 서버 검증·거래 중복 방지·취소 후 권한 갱신 |
| P0 | 고정 가격·오류 무시·수동 복원 부재 | `billing.ts`, `SettingsSheet.tsx` | 상품 조회, 명시적 상태, 복원 버튼 |
| P0 | 광고 로드를 노출로 취급하는 원안 | `AdInterstitialScreen.tsx`는 현재 화면 진입 시 기록 | SDK 표시 확인 이벤트에서 한 번만 기록 |
| P0 | 복원까지 구매처럼 집계 | `v_monetization`, `v_monetization_daily`는 source 구분 없음 | 구매·복원·테스트·레거시 분리 |
| P0 | 개인정보의 비식별/미수집 단정이 실제 구조와 불일치 | 문의 insert에 device_id·user_id·email 포함 | 실제 데이터 흐름으로 문서 재작성 |
| P0 | 소유자 준비 항목 중 비공개 테스트가 누락됨 | 새 개인 계정의 Play 요건 | 계정 적용 여부 확인 및 일정에 반영 |
| P1 | 전부 미커밋이라는 과거 상태 기록 | 해당 파일들은 git 추적 중 | 확인 시점과 사실로 변경 |
| P1 | 운영 데이터 전체 삭제 지시 | 원안 5-1 | 환경 분리·집계 제외 우선, 선택 삭제는 승인 후 |
| P1 | SDK 없음/무료 광고 제거라는 오래된 관리자 설명 | `admin/index.html`, `admin/README.md`, SQL 주석 | 사실과 집계의 한계를 함께 설명 |

**읽기 확인과 재현 확인을 구분한다.** 이번에 운영 서버에 이벤트를 보내거나 구매를 실행하지 않았다. 통계 인증 실패는 코드와 공식 인증 규칙상 수정이 필요한 위험이며, 현재 원격 환경에서 실제로 실패 중이라고 단정하지 않는다. 현재 로컬 타입 검사는 실패를 직접 확인했다.

## 3. 작업 규칙

1. 먼저 AGENTS.md와 현재 git diff/status를 읽는다. 기존 사용자 변경을 보존한다.
2. 이 문서를 작업 대상으로 사용하되 문서 속 예시 명령을 무조건 실행하지 않는다.
3. 의존성이 있는 작업은 아래 순서대로 진행한다. 매 단계 코드·검사·짧은 결과 기록을 남긴다.
4. 계정 정보가 없어도 독립적인 로컬 코드·mock 검증·문서는 끝까지 진행한다.
5. 구조를 크게 바꾸지 않는다. 기존 `billing.ts`·store·화면·SQL을 우선 활용한다. 별도 상태 관리 프레임워크나 범용 결제 플랫폼은 만들지 않는다.
6. 패키지는 Expo 54/RN 0.81.5 호환성을 확인해 필요한 것만 추가한다. 현재 API/타입을 확인하지 않고 예제를 복사하지 않는다.
7. 네이티브 모듈 추가 시 웹 import 경계를 점검한다. 타입 검사와 웹 번들이 네이티브 실행 성공을 증명하지는 않는다.
8. 비밀 키·영수증·purchaseToken·문의 본문·이메일은 분석 이벤트나 로그에 남기지 않는다.
9. 변경 SQL은 추가 migration 파일과 검증/되돌림 안내로 제공한다. 기존 운영 테이블 drop/recreate로 해결하지 않는다.
10. 외부 배포·실결제·운영 DB 변경/삭제·스토어 제출은 이 기획안 전달만으로 승인된 것으로 보지 않는다. 기존 대화에 명시적 승인이 있으면 그 범위는 존중한다.
11. 서브에이전트·다른 모델 실행은 이 명세에서 요구하지 않는다.
12. 완료 보고는 코드 검사 통과와 실기기/운영 검증을 분리한다. 외부 미완료가 있으면 출시 가능으로 표시하지 않는다.

## 4. 단계 A — 기준선과 환경 검증

**주요 파일:** `package.json`, `package-lock.json`, `app.json`, `eas.json`, 필요하면 `app.config.ts`, `.env.example`, 검증 스크립트.

### 수행

- `npm.cmd ci` 후 타입 검사를 다시 실행한다. 설치 실패라면 설치 문제를 먼저 보고하고 타입 선언을 날조하지 않는다.
- `npx.cmd expo install --check`, `npx.cmd expo-doctor`로 호환성을 확인한다. 무조건 전체 의존성 업그레이드 금지.
- 현행 버전·package·projectId를 확인한다. projectId/AdMob ID/운영 정보는 가짜로 생성하지 않는다.
- EAS 각 프로파일에 `environment`를 명시한다.
- 로컬용 예시 설정은 실제 키 없는 템플릿으로 추가한다.
- 런타임 공개 설정과 서버 비밀 설정을 분리한다.
- production 검증을 단순히 문서 명령으로만 두지 말고 실제 빌드/config 처리 경로에 연결한다.
- 웹 export/개발 실행에는 운영용 설정을 강제하지 않는다.

### 필요한 설정 계약

| 용도 | 설정 이름 제안 | 처리 |
|---|---|---|
| Supabase | 기존 EXPO_PUBLIC_SUPABASE_URL, EXPO_PUBLIC_SUPABASE_ANON_KEY 유지 | 공개 키 값은 미출력. 이름이 ANON_KEY여도 publishable 지원 |
| 개인정보 URL | EXPO_PUBLIC_PRIVACY_POLICY_URL | production HTTPS·빈칸/placeholder 검사 |
| AdMob Android 앱 ID | ADMOB_ANDROID_APP_ID | config plugin에서 사용 |
| 전면 광고 단위 | EXPO_PUBLIC_ADMOB_INTERSTITIAL_ANDROID_ID | 실제 광고를 켜는 production에서 필수 |
| 배포 환경 | EXPO_PUBLIC_APP_ENV | development/preview/production, 분석 분류용 |
| 구매 검증 서버 | 기존 Supabase 프로젝트의 서버 함수 경로 우선 | URL은 공개 가능, 서비스 계정 자격은 서버 전용 |

이름은 기존 관례에 맞춰 최소 변경해도 된다. `__DEV__`만으로 테스트/운영을 나누지 않는다. preview도 최적화된 release 빌드일 수 있다. 내부 테스트에서 production 환경을 사용한 테스트 구매는 서버의 스토어 테스트 판정으로 제외한다.

### 완료 조건

- 깨끗한 의존성 설치 후 타입 검사 성공 또는 별도 기존 오류가 정확히 분리됨.
- production 필수값을 하나씩 제거하면 빌드 검증이 설정 이름만 알려주며 실패함.
- development/preview에서는 공식 테스트 광고 ID 사용 가능, production에 테스트 ID를 남긴 상태로 일반 배포 불가.
- EAS 원격 설정은 조회/확인한 값만 완료 표시. 소유자 권한이 없으면 대기 표기.

## 5. 단계 B — Supabase 통계 전송과 접근 권한

**주요 파일:** `src/analytics/track.ts`, `src/lib/supabase.ts`, `supabase/schema.sql`, `admin/README.md`.

### 수행

- publishable 키를 Authorization Bearer에 넣지 않는다. 직접 REST 전송은 `apikey` 인증 방식으로 유지하고, 로그인 사용자 JWT가 필요한 요청과 구분한다.
- analytics를 문의용 로그인 세션에 무심코 붙이지 않는다. 현재 events INSERT 정책은 anon 역할만 허용하므로 인증 경로 변경 시 RLS와 함께 확인해야 한다.
- `response.ok`를 검사한다. fetch는 401/403/500도 reject하지 않을 수 있다.
- 개발 로그에는 이벤트명·HTTP 상태·비밀 없는 오류 분류만 남긴다. 통계 실패를 다시 track해 무한 재귀를 만들지 않는다.
- 게임 흐름은 네트워크/통계 실패와 무관하게 진행한다. 영구 큐나 무제한 재전송은 만들지 않는다.
- 신규 이벤트에 환경/스키마 버전 정보를 넣어 과거 더미와 구분한다. 서버 검증 없이 이 태그를 신뢰해 회계 데이터를 만들지는 않는다.
- 익명/일반 사용자/관리자의 실제 권한을 분리한다. 로그인 모달이 보안 경계라고 판단하지 않는다.

### 완료 조건

- publishable 헤더 구성, 401/500 진단, 네트워크 실패, 누락 설정 테스트.
- anon은 허용된 이벤트 INSERT만 가능하고 원본/관리자 집계는 조회 불가.
- 문의 사용자 A는 B의 문의 조회·메시지 입력 불가, sender=admin 위조 불가.
- 승인된 관리자만 집계와 문의 관리 가능.
- 원격 검증은 전용 테스트 데이터로 수행하고 운영 원본 삭제 없이 증거를 남김.

[Supabase API 키 규칙](https://supabase.com/docs/guides/getting-started/api-keys)

## 6. 단계 C — 결제 UX와 생명주기

**주요 파일:** `src/lib/billing.ts`, `src/state/store.ts`, `src/widgets/SettingsSheet.tsx`, `App.tsx`, 타이틀 구매 버튼 사용부.

### C1. 상품 조회와 가격

- 연결 후 `remove_ads` 상품을 조회한다. 설치한 expo-iap 버전의 조회 함수/상품 타입을 확인한다.
- 구매 버튼은 스토어가 제공하는 표시 가격을 쓴다. 통화·세금·지역이 반영된 값을 임의로 재조합하지 않는다.
- 상품을 못 찾으면 “상품 정보를 불러오지 못했어요”와 재시도 경로를 제공한다.
- 고정 2,200원으로 결제 가능하다고 표시하지 않는다.
- 웹/Expo Go에서 네이티브 결제가 불가능하면 구매 UI를 비활성화하고 지원 환경을 짧게 안내한다.

### C2. 요청과 결과

- 최소 상태: 초기화/준비/구매 중/결제 대기/검증 중/보유/오류를 사용자가 구분할 수 있게 한다. 구현은 기존 패턴에 맞춰 최소화.
- 구매 버튼 여러 위치에서 눌러도 한 건만 요청하도록 모듈 또는 기존 store 단위로 동시 실행 방지.
- `requestPurchase()` 반환과 실제 구매 완료를 구분한다. 버튼의 로컬 pending만 믿지 않는다.
- 취소는 조용히 원래 상태로 복귀하고, 결제 오류/상품 없음/연결 불가는 재시도 가능한 안내.
- pending에서는 광고 제거를 켜지 않는다.
- restore에서도 purchased 여부·상품·플랫폼 검증 경계를 동일하게 사용한다.
- 앱 시작·foreground 복귀 시 소유 내역 재조회를 통해 앱 종료 중 완료된 거래를 처리한다.
- listener 등록 중복을 막고 cleanup을 제공한다. 연결 실패 이후 재시도 시에도 listener가 빠지지 않게 한다.

### C3. 수동 복원·로컬 상태

- 설정에 “구매 복원” 버튼을 제공한다. 진행/복원됨/구매 없음/실패를 구분한다.
- 자동 복원과 수동 복원이 겹쳐도 거래 처리는 한 번.
- 기존 `td_ad_removed=true`는 검증된 영수증이 아니다. 예전에 무료로 켜졌던 값과 유효 구매를 구분할 마이그레이션이 필요하다.
- 마지막 서버 검증 결과를 캐시하되, 검증 전 기존 플래그를 영구 유료 권한으로 승격하지 않는다.
- 소유권 확인이 진행 중일 때 짧은 불확실성 때문에 구매자에게 광고를 급히 띄우지 않는다.
- 네트워크 오류와 “구매 없음/취소됨” 확정 응답을 구분한다. 오류 때문에 확인된 유료 권한을 즉시 삭제하지 않는다.
- **계획상 오프라인 기본값:** 이전에 검증된 권한은 검증 서버 장애 동안 유지하고 다음 foreground에 재시도한다. 이 경우 오프라인 환불 반영은 지연되므로 운영상 한계를 문서화한다.
- storage 저장 실패와 비동기 예외를 처리해 unhandled rejection이 발생하지 않게 한다.

### 완료 조건

- 성공/취소/오류/pending/연타/연결 재시도/재초기화/mock 복원 테스트.
- 앱 재시작·재설치·같은 Play 계정 다른 기기의 복원 시나리오를 실기기 테스트 목록에 포함.
- 구매 없음과 연결 실패가 서로 다른 안내를 보여줌.
- 구매 복원은 신규 유료 매출을 늘리지 않음.
- 고정 가격·버튼 직접 권한 부여·구매 상태 검사 없는 복원 경로가 사라짐.

## 7. 단계 D — 구매 검증과 승인, 최소 서버 구현

**목적:** 클라이언트 이벤트와 로컬 플래그만으로 유료 권한을 확정하지 않는다. 외부 결제 SaaS 도입이나 회원 시스템 재설계는 하지 않는다.

**파일 제안:** `supabase/functions/verify-purchase/index.ts`, 별도 migration, `billing.ts`, 필요 최소 서버 공통 코드. 실제 구현 전에 Supabase 함수 설정/현재 프로젝트 구조 확인.

### 서버 계약

1. 클라이언트는 상품 ID와 Play purchaseToken을 HTTPS 서버 함수로 보낸다. 앱 익명 인증을 재사용하되 재설치 시 새 익명 UID가 된다는 점을 고려한다.
2. 서버는 Google Play Developer API로 패키지·상품·구매 상태를 검증한다. API 자격증명은 서버 비밀 저장소에만 둔다.
3. 클라이언트가 보낸 purchased/test/price/source 값을 검증 결과처럼 신뢰하지 않는다.
4. 거래 원장에 토큰 기준 UNIQUE 제약 또는 동등한 영속 중복 방지를 둔다. 일반 이벤트 테이블에 토큰을 넣지 않는다.
5. 신규 거래인지 기존 거래 복원인지 서버가 판정한다. 재설치에서 복원한 첫 조회라도 원래 구매시각과 검증된 거래 기준으로 처리하며 복원일의 새 매출로 만들지 않는다.
6. 재설치로 익명 UID가 달라져도 적법한 Play 소유 구매 복원이 가능해야 한다. 최초 익명 UID에 영구 잠금하지 않는다. 동시에 임의 타인의 토큰 조회/원장 열람을 허용하지 않는다.
7. 서버 검증된 purchased에 한해서 권한을 부여한다. 미확인/위조/다른 상품/pending은 거부.
8. 구매 승인의 담당은 하나로 정한다. Android는 서버 acknowledge를 우선하며 expo-iap finishTransaction과의 관계를 확인해 중복 승인/소모 처리를 피한다. 상품은 절대 소비하지 않는다.
9. 권한 부여와 acknowledge 성공을 각각 기록한다. 승인 실패를 무시하고 끝내지 말고 영속 재시도 경로·작업 실행 방법을 제공한다.
10. 서버 자격 미설정 상태에서 “검증 성공” mock을 운영 모드에 남기지 않는다. 로컬 mock은 테스트 전용 경계에서만 사용한다.
11. 마지막 확인된 취소/환불 상태면 권한을 철회한다. 최소 범위는 시작/foreground/수동 복원 재조회이며, 앱 미사용 중 즉시 반영을 위한 RTDN은 후속 확장으로 따로 적는다.
12. 거래 원장 직접 접근은 일반 앱 사용자에게 금지한다. 인증·입력 길이 검증·호출 남용 제한을 마련하고 원문 토큰 로그 금지.
13. 서비스 계정/권한이 없으면 함수·migration·mock 검증은 완료하되 실제 판매 활성화는 대기로 남긴다.

거래 필드는 플랫폼·상품·검증 거래 식별·구매시각·상태·테스트 여부·승인 상태·최근 검증시각 정도로 제한한다. 금액을 신뢰할 수 있는 서버 응답/정산 자료에서 못 얻으면 “금액 미확인”으로 둔다. 클라이언트 가격을 실정산액으로 저장하지 않는다.

**주의:** 승인 미처리 구매는 자동 환불될 수 있다. pending에서 구매 완료로 전환되는 시점과 승인 기한은 실제 Play 규칙으로 검증한다. [Google 결제 통합](https://developer.android.com/google/play/billing/integrate), [구매 검증 권고](https://developer.android.com/google/play/billing/security)

### 완료 조건

- 동일 거래 동시 요청 2건 → 원장 1건, 권한 일관성 유지.
- 위조/다른 패키지/상품/pending → 권한 0.
- 서버 검증 성공·승인 실패 → 재시도 작업 존재, 앱 재설치에도 기록 유지.
- 이전 구매 복원 → 원장 중복 0, 새 구매 카운트 0.
- 테스트 구매 → 유료 수익 제외.
- 환불 확정과 일시적 서버 장애를 구분.
- 비소모성 상품 소비 API 호출 없음.
- 실제 Play API 테스트가 없으면 완료 보고에 “mock 검증만 완료” 명시.

## 8. 단계 E — 광고 SDK와 동의 흐름

**주요 파일:** `src/screens/AdInterstitialScreen.tsx`, `src/screens/BlindDateChatScreen.tsx`, `App.tsx`, `SettingsSheet.tsx`, app config, 필요 시 `src/lib/ads.ts`.

### 광고 흐름

- 호환되는 `react-native-google-mobile-ads`와 config plugin을 추가하고 개발 클라이언트를 재빌드한다.
- 네이티브 모듈은 웹에서 안전하게 대체되도록 파일 분리 또는 검증된 지연 import 사용.
- 광고 SDK는 전면 UI를 직접 표시한다. 더미 박스에 광고 컴포넌트를 끼워 넣는 방식으로 구현하지 않는다.
- 기존 5초 강제 카운트다운과 앱 자체 BackHandler 광고 잠금을 제거한다. SDK가 광고 닫힘 UI를 관리한다.
- 사전 로드를 사용한다면 동의 허용과 권한 확인 후에만 진행한다. 광고 제거 구매 직후 기존 preload가 떠버리지 않도록 show 직전 재확인.
- 결과 직전 광고가 준비되지 않았다면 **계획상 최대 3초 준비 대기** 후 결과로 진행한다. 3초는 제품 기본값으로 테스트 가능하게 구현하며 무제한 대기 금지.
- 이미 전체 화면 광고가 표시된 뒤에는 준비 타이머로 뒤에서 결과 화면을 열지 않는다.
- `CLOSED` → 결과 화면.
- 로드/표시 오류, no fill, 동의상 요청 불가, 지원되지 않는 환경 → 결과 화면.
- 여러 종료 이벤트가 겹쳐도 `navigation.replace`는 1회. listener와 timer는 정리.
- 화면 이탈 뒤 늦게 완료된 로드가 광고를 띄우지 않음.
- 이벤트 이름 `ad_shown` 유지 시 “앱에서 확인한 광고 표시”로 정의한다. 설치 버전의 실제 노출 이벤트를 확인하고, 없으면 `OPENED` 사용. 존재하지 않는 IMPRESSION 상수를 만들어 쓰지 않는다.
- `LOADED`에서는 ad_shown 0건. 같은 광고 표시에서 이벤트 1건.
- SDK 표시 이벤트는 AdMob 정산 수치와 차이가 날 수 있다고 대시보드에 명시.

[광고 표시 API](https://docs.page/invertase/react-native-google-mobile-ads/displaying-ads), [현재 이벤트 정의](https://github.com/invertase/react-native-google-mobile-ads/blob/main/src/AdEventType.ts)

### 동의와 테스트

- 앱 시작 시 UMP 동의 정보를 갱신하고 필요한 양식을 표시한다.
- 광고 요청 가능 여부를 확인한 뒤 로드/표시한다. 단순 “비개인화 광고” 플래그가 동의 절차를 대체한다고 가정하지 않는다.
- 필요한 지역/상태에서는 설정에 광고 개인정보 선택 UI 진입점을 제공한다.
- 동의 갱신 실패 시 SDK가 제공하는 기존 유효 상태/요청 가능 여부를 사용한다. 판단 불가이면 광고를 생략하고 게임은 진행.
- development/preview는 공식 테스트 광고만 사용한다. 실광고 클릭으로 테스트하지 않는다.
- production과 동일한 설치 환경 테스트 시 테스트 기기 설정을 사용하고, 배포 전에 디버그 지리/동의 reset/테스트 기기 설정을 검토한다.
- 출시 국가/연령 정보가 미정이면 콘솔 메시지 최종 설정은 대기로 남긴다.

[Google UMP](https://developers.google.com/admob/android/privacy), [라이브러리 동의 가이드](https://docs.page/invertase/react-native-google-mobile-ads/european-user-consent)

### 완료 조건

광고 없음/오프라인/동의 불가/SDK 없음/늦은 응답/중복 닫힘에서도 결과를 볼 수 있다. adRemoved 사용자에게 광고 요청·표시가 발생하지 않는다. 웹에서 광고 때문에 bundle/runtime 오류가 나지 않는다.

## 9. 단계 F — 수익 집계와 관리자 문구

**주요 파일:** `supabase/schema.sql`의 v_monetization/v_monetization_daily, migration, `admin/index.html`, `admin/README.md`.

### 집계 계약

| 데이터 | 의미 | 수익 처리 |
|---|---|---|
| ad_shown + 새 스키마/환경 | 실제 표시 확인 이벤트 | 노출 기반 추정 광고 수익만 가능 |
| remove_ads, source=purchase | 클라이언트 구매 처리 관측 | 참고 전환 지표. 단독으로 실매출 아님 |
| remove_ads, source=restore | 기존 권한 복원 관측 | 신규 구매/수익 제외 |
| 서버 검증 원장, 운영 구매 | 고유한 검증 거래 | 거래 수 기준으로 활용. 정산액은 별도 확인 |
| 서버 검증 원장, 테스트 구매 | Play 라이선스 테스트 등 | 실제 유료 수익 제외 |
| source/버전/환경 없는 과거 이벤트 | 더미 또는 분류 불가 | 보존, 새 유료 집계 제외 |

- 클라이언트 환경 표식은 분석 분류를 위한 것이며 보안 검증이 아니다.
- 구매 원장은 콜백 반복·앱 재설치·복원으로 건수가 늘지 않아야 한다.
- 서버 검증 연동이 준비되기 전에는 매출 카드에 “미연동/검증 대기”로 표시한다. 데이터를 못 읽은 것과 수익 0을 구분한다.
- 고정 가격×구매 수는 필요하면 **추정 총액**으로만 표시. 서로 다른 통화는 합산하지 않는다.
- 세금·수수료·환불을 반영하지 않은 값을 “정산액/실매출”로 표시하지 않는다.
- 소규모 프로그램 수수료 같은 적용 여부 미확인 수치를 확정 사실로 쓰지 않는다.
- 기존 total/daily의 필터 정의를 통일한다. 관리자 그래프가 기대하는 컬럼을 함께 변경한다.
- view 컬럼 변경이 CREATE OR REPLACE 제한에 걸리는지 확인하고 의존성을 포함한 migration을 작성한다.
- security_invoker와 관리자 제한을 유지한다.
- 과거 이벤트를 일괄 삭제하거나 가짜 purchase source를 채우지 않는다.
- 광고 eCPM은 추정 입력값이며 최신 AdMob 보고서와 별도로 관리한다.

### 완료 조건

고정 fixture: 운영 검증 구매 1건, 그 거래 재처리 1회, 복원 2회, 테스트 구매 1건, 레거시 이벤트 1건 → 신규 운영 거래 1건. 이력이 없는데 복원부터 수집된 경우도 복원 당일 신규 거래로 과대 집계하지 않는다. 현재 뷰와 일별 뷰의 정의가 일치하며 관리자 권한 없는 사용자는 조회 불가.

## 10. 단계 G — 개인정보와 설정 화면

**주요 파일:** `PRIVACY_POLICY.md`, `src/widgets/SettingsSheet.tsx`, 문의 화면/문구, 운영 문서.

### 문서 수정

- 게임 이름은 로컬 입력, 이메일은 문의 시 선택 전송이라고 구분한다.
- 임의 UUID도 문의 이메일·익명 UID와 연결될 수 있다는 사실을 명시한다.
- 플레이 결과 중 서버로 보내는 항목과 기기에만 남는 원본/상세 기록을 구분한다.
- 현재 광고 제거 이벤트는 버튼 클릭이 아니라 결제/복원 처리 기록임을 반영한다.
- 구매 서버 검증을 구현하면 토큰/거래 식별·목적·접근 제한·보유 정책도 반영한다.
- 오류 메시지에 사용자 입력/개인정보가 섞이지 않도록 수집 코드와 함께 점검한다.
- 이메일 주소가 선택 항목이므로 “연락처를 수집하지 않는다”는 포괄 문구를 수정한다.
- 이메일 자동 발송 코드는 확인하지 못했다. 외부 수동 답변 절차가 없다면 이메일 답변 보장 문구를 삭제한다. 새 이메일 서비스 도입은 범위 밖.
- 자동 파기 job이 없으면 자동 파기라고 쓰지 않는다. 소유자가 정한 보유 기간에 맞는 수동/자동 운영 절차와 실행 담당을 명시한다.
- 네트워크 차단만을 개인정보 거부 방법이라고 안내하지 않는다. 실제 가능한 권리행사 경로를 적고, 별도 동의가 필요한 처리는 출시 전에 정책/법률 검토 사항으로 남긴다.
- 삭제 요청의 대상 식별 방법을 마련한다. 기기 구분값을 알려달라고 한다면 사용자가 안전하게 확인/전달할 수 있는 경로도 있어야 한다.
- 삭제/보유 관련 운영 약속은 승인된 사실만 채우고 책임자/기간/리전/날짜를 추정하지 않는다.
- 앱 삭제와 서버 데이터 삭제를 구분하고 OS 백업 복원 가능성을 무시한 즉시·완전 삭제 보장을 피한다.
- AdMob 설치만으로 iOS Tracking=Yes/ATT 필수라고 쓰지 않는다. iOS 출시 시 실제 추적 용도에 따라 검토한다.
- 타깃 국가/연령·Data Safety 항목은 구현과 SDK 공식 수집 안내를 대조해 소유자 확인 표로 제공한다.

### 화면 수정

- 설정에서 개인정보처리방침 외부 링크를 열고 링크 실패를 안내한다.
- 개발 환경에서 URL 미설정 시 안내, production에서는 설정 검증으로 차단.
- 수동 구매 복원·광고 개인정보 선택 UI를 추가해도 작은 화면/큰 글씨에서 스크롤로 접근 가능해야 한다.
- 현재 테마·사운드·문의 버튼의 위치/기능을 가능한 유지한다.
- 익명 계정 사용이 스토어 계정 삭제 요건에 해당하는지 실제 앱 흐름과 정책을 확인한다. 해당하면 필요한 앱/웹 삭제 경로를 출시 차단 항목으로 추가하고, 미확인 상태로 준수했다고 표기하지 않는다.

공개 정책은 기술 검토 초안이며 법률 검토 완료를 뜻하지 않는다. [Google 사용자 데이터 정책](https://support.google.com/googleplay/android-developer/answer/10144311?hl=en), [Apple 개인정보/추적 기준](https://developer.apple.com/app-store/user-privacy-and-data-use/)

## 11. 단계 H — 검증·운영·인수인계

### 검사 명령

```powershell
npm.cmd ci
npm.cmd run typecheck
npx.cmd expo install --check
npx.cmd expo-doctor
npx.cmd expo export --platform web --output-dir dist
git diff --check
```

명령마다 실제 종료코드와 주요 결과를 기록한다. 마지막 명령 성공으로 앞 명령 실패를 덮지 않는다. export 산출물은 커밋하지 않는다. 현재 테스트 러너는 package.json에 없으므로 결제/광고/집계 회귀를 검증할 최소 테스트 도구만 선택한다. 문구 변경 자체를 테스트하는 과도한 테스트는 만들지 않는다.

### 필수 검증 매트릭스

| ID | 환경/조건 | 기대 결과 |
|---|---|---|
| ENV-01 | production 필수 설정 누락 | 빌드 차단, 민감값 출력 없음 |
| ENV-02 | 웹/Expo Go에서 네이티브 SDK 없음 | 게임 정상, 광고 생략, 구매 불가 안내 |
| API-01 | publishable 키 전송 | 잘못된 Bearer 없음, 성공 HTTP 확인 |
| API-02 | 통계 401/500/offline | 개발 진단 존재, 게임 진행 |
| IAP-01 | 정상 구매 | 서버 검증 후 권한, 중복 거래 없음 |
| IAP-02 | 사용자 취소/스토어 오류 | 미구매 유지, 요청 잠금 해제 |
| IAP-03 | pending→앱 종료→구매 완료 | 재진입 조회로 처리, pending 중 권한 없음 |
| IAP-04 | 여러 구매 버튼 연타 | 요청 한 건 |
| IAP-05 | 재설치/같은 Play 계정 복원 | 권한 복원, 새 매출 없음 |
| IAP-06 | 재연결/리스너 재등록 | 중복 처리 없음, 다음 시도 성공 가능 |
| IAP-07 | 검증 실패/위조/다른 상품 | 권한 없음, 민감 토큰 로그 없음 |
| IAP-08 | 승인 실패 | 영속 재시도, 무조건 완료로 보고하지 않음 |
| IAP-09 | 환불/구매 없음 확정 | 다음 성공한 검증에서 권한 갱신 |
| IAP-10 | 서버 장애+이전 검증 권한 | 기본 오프라인 정책대로 유지/재시도 |
| ADS-01 | load만 성공 | ad_shown 0 |
| ADS-02 | 표시 후 닫힘 | ad_shown 1, 결과 전환 1 |
| ADS-03 | no fill/error/동의상 불가 | 불필요한 5초 대기 없이 결과 |
| ADS-04 | 준비 시간 초과 뒤 늦은 load | 결과 유지, 뒤늦은 광고 없음 |
| ADS-05 | 닫힘+에러 중복 | 결과 전환 1 |
| ADS-06 | adRemoved 또는 소유권 확인 중 | 구매자 광고 노출 방지 |
| ADS-07 | 동의 양식/설정 재선택 | SDK 요청 가능 상태 준수 |
| DATA-01 | 구매/복원/테스트/레거시 fixture | 운영 구매만 집계 |
| SEC-01 | anon/사용자 A/사용자 B/관리자 | 원본 보호·문의 격리·관리자 구분 |
| UI-01 | 큰 글씨/작은 화면/다크 모드 | 복원/정책/문의 접근 가능 |
| GAME-01 | 양쪽 스토리 한 회차 이상 완료 | 광고 유무와 무관하게 결과 보존 |
| PRIV-01 | 공개 URL/삭제 요청 안내 | 로그인 없이 열림, 실제 연락 경로 존재 |

mock 통과와 스토어 실기기 통과는 각각 별도 열로 기록한다. 테스트 구매 성공을 실결제/정산 성공이라고 쓰지 않는다.

### 소유자 입력표

| 필요한 값/상태 | 없을 때 할 수 있는 작업 | 출시 전 증거 |
|---|---|---|
| Play 계정/프로덕션 접근 | 로컬 개발·테스트 시나리오 | 계정 조건·접근 승인 |
| 상품 판매 상태/라이선스 계정 | 결제 mock·상품 오류 UX | 내부 테스트 구매/복원 결과 |
| Expo 프로젝트/환경 | config·검증 스크립트 | 실제 빌드 성공과 설정 확인 |
| Play API 서비스 계정 권한 | 검증 함수·mock·migration | 실제 거래 검증/승인 |
| AdMob ID·동의 메시지 | 테스트 광고 통합 | 실제 콘솔 준비 상태·기기 테스트 |
| 운영자/연락처/보유 기간/리전 | 빈칸 있는 정책 초안 | 소유자 확정 |
| 개인정보 공개 URL | 링크 UI·누락 방지 | URL 접근 및 게시 본문 일치 |
| 출시 국가/대상 연령 | 국가별 미확정 표기 | 콘솔/방침/광고 설정 일치 |

### 데이터 정리

- 테스트용 환경/프로젝트 분리를 우선한다. 같은 운영 프로젝트를 쓸 경우 테스트 이벤트와 검증된 테스트 구매를 명시적으로 제외한다.
- 과거 데이터의 테스트 여부를 증명할 수 없으면 보존하고 새 수익 집계에서 레거시로 분류한다.
- 출시 전에 데이터가 반드시 0건이어야 하는 것은 아니다.
- 선택 삭제가 요청되면 먼저 대상 SELECT·건수·PK 목록·백업·FK cascade 영향·되돌림 방법을 제시한다.
- 타임스탬프만으로 테스트 사용자라고 단정하지 않는다.
- auth.users의 모든 익명 계정 삭제는 하지 않는다. 삭제 SQL 자동 실행/예약도 하지 않는다.
- 이번 범위에서 자동화/스케줄러를 실제 등록하지 않는다. 보유/승인 재시도 운영은 실행 코드와 등록 절차까지 준비하고 외부 적용은 승인 범위에 따른다.

## 12. 산출물과 완료 판정

후속 실행자는 다음을 남긴다.

1. 요청 범위의 코드·설정 변경.
2. DB migration과 적용 순서/되돌림/권한 테스트 방법.
3. `LAUNCH_CHECKLIST.md`, `PRIVACY_POLICY.md`, `admin/README.md`의 사실 정합성 갱신.
4. `LAUNCH_VERIFICATION.md`: 실행한 명령, mock 결과, 실기기 빌드/버전·결과, 미검증 항목, 소유자 작업.
5. 핵심 회귀 테스트와 재현 명령.
6. 최종 짧은 보고: 바뀐 점, 검증 근거, 남은 출시 차단 요인.

**코드 준비 완료:** 로컬 검사/회귀 테스트 통과, 오류·미지원 환경 처리, 보안 경계·migration 준비, 문서 일치.
**출시 검증 완료:** 실제 Play 결제/복원·광고·동의·RLS·공개 방침·필수 계정 요건까지 증거 있음.
**출시 승인:** 소유자의 별도 결정. 코드 준비 완료를 출시 승인으로 대체하지 않는다.

심사 소요 시간을 확정하거나 “오늘 바로 출시 가능”이라고 약속하지 않는다. 비공개 테스트 대상이면 최소 참여 기간과 접근 심사를 개발 일정과 별도로 관리한다. [Play 신규 개인 계정 요건](https://support.google.com/googleplay/android-developer/answer/14151465)

## 13. 전달용 시작 프롬프트

아래 블록만 복사해도 된다. 위 문서가 같은 프로젝트에 있어야 한다.

```text
C:\1\TYPE-DATE-RN 프로젝트의 Android 출시 준비를 구현해줘.

먼저 AGENTS.md, LUNA_LAUNCH_IMPLEMENTATION_PLAN.md,
수정된 LAUNCH_CHECKLIST.md와 현재 git status/diff를 읽어.
이전 사용자의 변경은 보존하고, 계획의 A→H 순서로 진행해.
모델은 사용자가 선택한 GPT-5.6 Luna, reasoning은 extra high(xhigh)를 전제로 해.
프롬프트만으로 모델 설정을 바꾸었다고 말하지 마.

목표는 계획 설명이 아니라 실제 코드·설정·migration·필수 회귀 검증까지 완료하는 거야.
결제/복원/서버 검증, 광고와 동의, Supabase 통계 인증,
구매·복원·테스트 수익 분리, 개인정보·설정 화면을 계획대로 고쳐줘.

첫 단계는 lock 기준 의존성 설치 후 타입 검사야.
현재 expo-iap 미설치 오류를 any나 가짜 타입으로 숨기지 마.
스토어 상품 가격을 사용하고 pending이나 검증 실패에 광고 제거 권한을 주지 마.
restore를 새 매출로 세지 말고, 광고 load를 노출로 세지 마.
서버·SDK API는 설치 버전과 공식 문서를 확인해.

계정 정보가 없어도 로컬 구현, 테스트 전용 mock, 문서, migration은 진행해.
가짜 ID/비밀값/운영 검증 성공 응답을 만들지 마.
외부 배포·운영 DB 변경/삭제·실결제·스토어 제출은
기존 대화의 명시적 승인 범위가 없으면 검토 가능한 결과를 준비한 뒤 승인받아.
전체 이벤트나 익명 계정을 삭제하지 마.

SNS 공유, iOS 출시, 게임 콘텐츠/디자인 개편은 하지 마.
필요한 파일만 수정하고 대규모 리팩터링이나 추가 서비스 도입은 피해야 해.
서브에이전트나 별도 작업을 만들 필요는 없어.

단계별 실제 검사 결과를 LAUNCH_VERIFICATION.md에 기록해.
마지막에 변경 파일, 통과한 검사, 실기기 미검증 사항,
소유자가 처리할 항목과 출시 차단 요인을 구분해서 한국어로 보고해.
코드 준비 완료를 실기기 검증 완료나 출시 가능이라고 과장하지 마.
```
